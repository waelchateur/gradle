plugins {
    id 'java-library'
    id 'maven-publish'
    id 'com.github.johnrengelman.shadow' version '8.1.1'
}

tasks.assemble.dependsOn tasks.shadowJar
tasks.build.dependsOn tasks.shadowJar

shadowJar {
    exclude '**/*-classpath.properties'
    exclude '**/*-names.properties'
    exclude '**/*package-info.class'

    exclude '**/*org/gradle/launcher/bootstrap/ProcessBootstrap*'
    exclude '**/*org/gradle/launcher/daemon/client/DefaultDaemonStarter*'
    exclude '**/*org/gradle/launcher/daemon/bootstrap/GradleDaemon*'
    exclude '**/*org/gradle/launcher/daemon/configuration/DaemonParameters*'

    archiveFileName.set("${project.name}.jar")
}

dependencies {
    implementation files (
        'libs/gradle-launcher-7.5.jar'
    )
}

publishing {
    publications {
        shadow(MavenPublication) { publication ->
            groupId = 'org.kodtik.ide'
            artifactId = 'launcher'
            version = '7.5-rc-1'
            from project.shadow.component(publication)
        }
    }
}
